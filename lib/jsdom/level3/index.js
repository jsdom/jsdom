exports.dom = {
  level3 : {
    core   : require(__dirname + "/core").dom.level3.core,
    events : require(__dirname + "/events").dom.level3.events,
    html   : require(__dirname + "/html").dom.level3.html
  }
};